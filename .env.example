# Kyara Configuration
# Copy this file to .env and adjust values as needed

# Application name (used in Prometheus labels)
KYARA_APP_NAME=kyara-puppet

# Path to YAML scenario file
KYARA_YAML_FILE_PATH=/var/config/kyara.yaml

# HTTP server port (0 = random port)
KYARA_HTTP_PORT=3000

# Prometheus metrics endpoint
KYARA_HTTP_METRICS_ROUTE=/metrics

# Health check endpoint
KYARA_HTTP_LIVENESS_PROBE_ROUTE=/live

# Run browser in headless mode (true/false)
KYARA_HEADLESS=true
